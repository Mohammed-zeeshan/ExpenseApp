<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Expense</title>
</head>
<body>
    <h1>Expense Tracker App</h1>
    <form action="/editexpense" method="POST">
        <input type="hidden" name="expenseId" value="<%= expense.id%>">
        <div><label for="amount">Amount</label></div>
        <div><input type="number" value="<%= expense.amount%>" name="amount" id="amount" required></div>
        <div><label for="description">Description</label></div>
        <div><input type="text" value="<%= expense.description%>" name="description" id="description" required></div>
        <div><label for="category">Choose category</label></div>
        <div>
            <select name="category" id="category">
                <option value="<%= expense.category%>"><%= expense.category%></option>
                <option value="food">Food</option>
                <option value="fuel">Fuel</option>
                <option value="bills">Bills</option>
            </select>
        </div>
        <div><button type="submit">Edit</button></div>
    </form>
</body>
</html>